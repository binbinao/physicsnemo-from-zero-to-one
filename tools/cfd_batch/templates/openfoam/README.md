# OpenFOAM case template (placeholder)

Replace with your organization's motorBike / simpleFoam / SNAPPY workflow.

Expected outputs in case work_dir:

- `result_cd.txt` — single line: `cd=0.251` or `0.251`
- Optional: VTK fields for V&V

See `Allrun.template` for command skeleton.
